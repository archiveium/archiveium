APP_URL=http://localhost
APP_FORCE_HTTPS=false

##
# Information below will be used to create first user account
##
# display name
ADMIN_NAME=
# account username (used for logging in)
ADMIN_EMAIL=
# account password
ADMIN_PASSWORD=

##
# Database credentials
##
DB_DATABASE=archiveium
DB_USERNAME=archiveium
DB_PASSWORD=archiveiumPassword

##
# Minio credentials
#
MINIO_USER=minio
MINIO_PASSWORD=minioPassword

##
# Honeypot configuration
##
HONEYPOT_ENABLED=true
HONEYPOT_NAME=welcome_dear
HONEYPOT_RANDOMIZE=true
HONEYPOT_VALID_FROM_TIMESTAMP=true
HONEYPOT_VALID_FROM=welcome_since
HONEYPOT_SECONDS=3

##
# SMTP configuration
##
MAIL_MAILER=smtp
MAIL_HOST=
MAIL_PORT=
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=
MAIL_FROM_ADDRESS=
MAIL_FROM_NAME="${APP_NAME}"

##
# DO NOT EDIT BELOW THIS LINE
# UNLESS YOU KNOW WHAT YOUR ARE DOING
# YOU HAVE BEEN WARNED
##
# App Key will be generated automatically
APP_KEY=
APP_NAME=Archiveium
APP_ENV=production
APP_DEBUG=false

LOG_CHANNEL=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

AWS_URL=http://minio:9000
AWS_ENDPOINT=http://minio:9000
AWS_ACCESS_KEY_ID=minioAccessKey
AWS_SECRET_ACCESS_KEY=minioSecretAccessKey
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=emails
AWS_USE_PATH_STYLE_ENDPOINT=true

BATCH_SIZE=100

#MEMCACHED_HOST=127.0.0.1

#REDIS_HOST=127.0.0.1
#REDIS_PASSWORD=null
#REDIS_PORT=6379

#PUSHER_APP_ID=
#PUSHER_APP_KEY=
#PUSHER_APP_SECRET=
#PUSHER_APP_CLUSTER=mt1

#MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
#MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

#MEILISEARCH_HOST=
#MEILISEARCH_PORT=
#MEILISEARCH_MASTER_KEY=
#MEILISEARCH_INDEX=
